<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geowidget">

    <!-- ============================= -->
    <!-- Permissions                  -->
    <!-- ============================= -->

    <!-- Read network / Wi-Fi state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Toggle Wi-Fi (system / priv-app) -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NETWORK_SETTINGS" />

    <!-- ============================= -->
    <!-- Application                  -->
    <!-- ============================= -->

    <application
        android:label="Geo WiFi Widget"
        android:allowBackup="false"
        android:persistent="true">

        <!-- ================================= -->
        <!-- MainActivity (bootstrap only)    -->
        <!-- Starts NetworkCallback service   -->
        <!-- ================================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ================================= -->
        <!-- Persistent Network Observer      -->
        <!-- Holds ConnectivityManager callback -->
        <!-- ================================= -->
        <service
            android:name=".WifiNetworkService"
            android:exported="false" />

        <!-- ================================= -->
        <!-- AppWidget Provider (DUMB VIEW)   -->
        <!-- NO system broadcasts here        -->
        <!-- ================================= -->
        <receiver
            android:name=".WifiWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/wifi_widget_info" />
        </receiver>

        <!-- ================================= -->
        <!-- Widget click → toggle Wi-Fi      -->
        <!-- ================================= -->
        <receiver
            android:name=".WifiToggleReceiver"
            android:exported="false" />

    </application>

</manifest>
